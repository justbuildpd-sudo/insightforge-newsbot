# Synology 웹 인터페이스 접속 및 NewsAnalyzer 문제 해결 가이드

## 📅 작성 날짜: 2024-10-23

## 🔐 접속 정보

**Synology DSM 접속:**
- **URL**: http://192.168.219.2:5000
- **사용자**: admin
- **비밀번호**: Jsjs8078

## 📊 NewsAnalyzer 상태 확인 방법

### 1. DSM 로그인
1. 웹 브라우저에서 `http://192.168.219.2:5000` 접속
2. 사용자명: `admin`
3. 비밀번호: `Jsjs8078`
4. 로그인 버튼 클릭

### 2. Docker 상태 확인
1. **패키지 센터** 클릭
2. **Docker** 패키지 찾기
3. **열기** 버튼 클릭
4. **컨테이너** 탭 선택
5. **NewsAnalyzer** 관련 컨테이너 확인

### 3. 컨테이너 상태 확인
다음 컨테이너들이 있는지 확인:
- `newsanalyzer-historical`
- `newsanalyzer-daily`
- `api-management`

## 🔧 문제 해결 방법

### 1. 컨테이너가 중지된 경우
**증상**: 컨테이너 상태가 "중지됨" 또는 "Exited"
**해결책**:
1. 컨테이너 선택
2. **시작** 버튼 클릭
3. 상태가 "실행 중"으로 변경되는지 확인

### 2. 컨테이너가 오류 상태인 경우
**증상**: 컨테이너 상태가 "오류" 또는 "Error"
**해결책**:
1. 컨테이너 선택
2. **로그** 탭 클릭
3. 오류 메시지 확인
4. **재시작** 버튼 클릭

### 3. API 한도 초과인 경우
**증상**: "API limit exceeded" 또는 "Quota exceeded" 오류
**해결책**:
1. `api-management` 컨테이너 확인
2. 웹 인터페이스 접속: `http://192.168.219.2:5001`
3. **API 리셋** 버튼 클릭
4. 사용량 초기화 확인

### 4. 리소스 부족인 경우
**증상**: "Out of memory" 또는 "Resource limit exceeded" 오류
**해결책**:
1. 컨테이너 선택
2. **편집** 버튼 클릭
3. **고급 설정** → **리소스** 탭
4. 메모리 제한 증가 (예: 2GB → 4GB)
5. **적용** 버튼 클릭

## 📈 모니터링 방법

### 1. 실시간 상태 확인
1. Docker → 컨테이너 탭
2. NewsAnalyzer 컨테이너 선택
3. **통계** 탭에서 CPU/메모리 사용량 확인
4. **로그** 탭에서 실시간 로그 확인

### 2. 데이터 수집 진행률 확인
1. **파일 스테이션** 앱 열기
2. `/volume1/docker/NewsAnalyzer/output/lda_results/` 경로로 이동
3. `local_politicians_lda_analysis.json` 파일 확인
4. 파일 크기와 수정 시간 확인

### 3. API 사용량 확인
1. `api-management` 컨테이너 선택
2. 웹 인터페이스 접속: `http://192.168.219.2:5001`
3. API 사용량 및 한도 확인
4. 필요시 API 리셋 실행

## 🚀 자동화 설정

### 1. 컨테이너 자동 재시작 설정
1. 컨테이너 선택
2. **편집** 버튼 클릭
3. **고급 설정** → **자동 재시작** 탭
4. **항상 재시작** 선택
5. **적용** 버튼 클릭

### 2. 로그 로테이션 설정
1. **제어판** → **로그 센터**
2. **로그 설정** 탭
3. **로그 로테이션** 활성화
4. **최대 크기**: 100MB
5. **보관 기간**: 7일

### 3. 알림 설정
1. **제어판** → **알림**
2. **이메일** 또는 **SMS** 설정
3. **Docker 컨테이너 중지** 알림 활성화
4. **API 한도 초과** 알림 활성화

## 📊 성능 최적화

### 1. 리소스 할당 최적화
**권장 설정**:
- **메모리**: 2GB 이상
- **CPU**: 1코어 이상
- **디스크**: 10GB 이상

### 2. 네트워크 설정 최적화
1. **제어판** → **네트워크**
2. **DNS 서버** 설정 확인
3. **방화벽** 설정에서 필요한 포트 열기
4. **포트 전달** 설정 확인

### 3. 스토리지 최적화
1. **저장소 관리자** 앱 열기
2. **SSD 캐시** 설정 확인
3. **디스크 사용량** 모니터링
4. **불필요한 파일** 정리

## 🎯 문제 해결 체크리스트

### ✅ 기본 확인사항
- [ ] DSM 로그인 성공
- [ ] Docker 패키지 설치됨
- [ ] NewsAnalyzer 컨테이너 존재
- [ ] 네트워크 연결 정상

### ✅ 컨테이너 상태 확인
- [ ] 컨테이너 실행 중
- [ ] 오류 메시지 없음
- [ ] 리소스 사용량 정상
- [ ] 로그에 오류 없음

### ✅ API 상태 확인
- [ ] API 키 유효
- [ ] 사용량 한도 내
- [ ] 네트워크 연결 정상
- [ ] 응답 시간 정상

### ✅ 데이터 수집 확인
- [ ] 수집 스크립트 실행 중
- [ ] 데이터 파일 생성됨
- [ ] 파일 크기 증가 중
- [ ] 오류 로그 없음

## 🚨 긴급 복구 방법

### 1. 전체 시스템 재시작
1. **제어판** → **전원 관리**
2. **시스템 재시작** 클릭
3. 재시작 완료 후 Docker 상태 확인

### 2. Docker 서비스 재시작
1. **패키지 센터** → **Docker**
2. **중지** 버튼 클릭
3. **시작** 버튼 클릭
4. 컨테이너 상태 확인

### 3. 데이터 백업 및 복구
1. **하이퍼백업** 앱 사용
2. **로컬 폴더** 백업 설정
3. **정기 백업** 스케줄 설정
4. **복구** 시 백업 파일 사용

## 📞 지원 정보

### 접속 정보
- **Synology IP**: 192.168.219.2
- **DSM 포트**: 5000
- **Docker 포트**: 5001 (API 관리)
- **사용자**: admin
- **비밀번호**: Jsjs8078

### 유용한 링크
- **DSM**: http://192.168.219.2:5000
- **Docker**: http://192.168.219.2:5000/docker
- **API 관리**: http://192.168.219.2:5001
- **파일 스테이션**: http://192.168.219.2:5000/file

## 🎉 결론

**NewsAnalyzer 문제 해결을 위해서는:**

1. **웹 브라우저에서 DSM 접속**
2. **Docker 컨테이너 상태 확인**
3. **문제에 따른 적절한 조치 실행**
4. **지속적인 모니터링 설정**

**이 가이드를 따라 NewsAnalyzer 수집 실패 문제를 해결할 수 있습니다.**

---
*이 문서는 Synology 웹 인터페이스를 통한 NewsAnalyzer 문제 해결 가이드입니다.*
