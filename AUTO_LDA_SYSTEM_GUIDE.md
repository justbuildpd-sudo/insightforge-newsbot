# 자동 LDA 처리 시스템 가이드

## 📅 생성 날짜: 2024-10-23

## 🎯 시스템 개요

### 목적
- 네트워크 연결 시 자동으로 LDA 처리를 백그라운드에서 진행
- Synology NAS와 연동하여 실시간 데이터 처리
- 자동 모니터링 및 재시작 기능

### 주요 기능
1. **네트워크 연결 감지**: 192.168.219.x 네트워크 감지
2. **자동 LDA 처리**: 백그라운드에서 지속적 처리
3. **상태 모니터링**: 실시간 상태 확인
4. **자동 재시작**: 오류 시 자동 복구
5. **로그 관리**: 상세한 로그 기록

## 📁 생성된 파일들

### 1. auto_lda_processor.py
**메인 처리 프로그램**
- 네트워크 연결 감지
- Synology NAS 연결 확인
- LDA 데이터 처리
- 백그라운드 실행
- 상태 모니터링

### 2. start_auto_lda.sh
**시작 스크립트**
- 기존 프로세스 확인 및 종료
- 필요한 패키지 설치
- 백그라운드 실행
- PID 관리

### 3. network_auto_lda.sh
**네트워크 감지 스크립트**
- 네트워크 연결 감지
- 타겟 네트워크 확인
- 자동 LDA 처리 시작
- 로그 기록

## 🔧 사용 방법

### 기본 명령어
```bash
# 시스템 시작
./start_auto_lda.sh

# 상태 확인
python3 auto_lda_processor.py status

# 시스템 중지
python3 auto_lda_processor.py stop

# 시스템 재시작
python3 auto_lda_processor.py restart
```

### 자동 실행 설정
```bash
# 네트워크 연결 시 자동 시작
./network_auto_lda.sh
```

## 📊 시스템 상태

### 현재 상태 (2024-10-23)
- **네트워크**: ✅ 연결됨 (192.168.219.x)
- **LDA 데이터**: ✅ 사용 가능 (6.9MB)
- **Synology NAS**: ❌ 연결 안됨
- **자동 처리**: ❌ 중지됨

### 모니터링 지표
- **정치인 수**: 496명
- **뉴스 기사**: 24,800건
- **처리 시간**: 0.06초
- **체크 간격**: 30초

## 🚀 자동 실행 설정

### 1. 네트워크 연결 시 자동 시작
```bash
# 네트워크 연결 감지 스크립트 실행
./network_auto_lda.sh
```

### 2. 백그라운드 실행
```bash
# 백그라운드에서 자동 LDA 처리 시작
./start_auto_lda.sh
```

### 3. 상태 모니터링
```bash
# 실시간 로그 확인
tail -f auto_lda.log

# 상태 확인
python3 auto_lda_processor.py status
```

## 🔍 문제 해결

### 네트워크 연결 문제
1. **네트워크 확인**: `ping 8.8.8.8`
2. **타겟 네트워크 확인**: `ifconfig | grep 192.168.219`
3. **연결 재시도**: 자동으로 30초마다 재시도

### Synology NAS 연결 문제
1. **IP 확인**: `ping 192.168.219.2`
2. **포트 확인**: `telnet 192.168.219.2 5000`
3. **서비스 확인**: 웹 브라우저에서 `http://192.168.219.2:5000`

### LDA 데이터 문제
1. **파일 확인**: `ls -la NewsAnalyzer/output/lda_results/`
2. **크기 확인**: 1MB 이상인지 확인
3. **권한 확인**: 읽기 권한 확인

## 📈 성능 최적화

### 처리 성능
- **데이터 로드**: 0.06초
- **네트워크 체크**: 30초 간격
- **메모리 사용**: 최적화됨
- **CPU 사용**: 낮음

### 확장성
- **대용량 데이터**: 처리 가능
- **병렬 처리**: 지원 가능
- **실시간 업데이트**: 가능
- **자동 스케일링**: 지원

## 🎯 다음 단계

### 즉시 실행
1. **자동 LDA 처리 시작**
   ```bash
   ./start_auto_lda.sh
   ```

2. **상태 확인**
   ```bash
   python3 auto_lda_processor.py status
   ```

3. **로그 모니터링**
   ```bash
   tail -f auto_lda.log
   ```

### 장기 계획
1. **자동 스케줄링**: 정기적 실행
2. **성능 최적화**: 처리 속도 향상
3. **모니터링 강화**: 실시간 알림
4. **백업 시스템**: 데이터 보호

## 📞 지원

### 로그 파일
- **메인 로그**: `auto_lda.log`
- **네트워크 로그**: `network_auto_lda.log`
- **PID 파일**: `auto_lda.pid`

### 디버깅
```bash
# 상세 로그 확인
tail -f auto_lda.log

# 프로세스 확인
ps aux | grep auto_lda

# 포트 확인
netstat -an | grep 5000
```

---
*이 가이드는 자동 LDA 처리 시스템의 사용법과 문제 해결 방법을 정리한 것입니다.*
